# Input

```tsx
import React from 'react';
import { Input } from 'react-mylib';

export default () => {
  const styles = { display: 'flex', justifyContent: 'space-between' };

  return (
    <div style={styles}>
      <Input />
    </div>
  );
};
```
